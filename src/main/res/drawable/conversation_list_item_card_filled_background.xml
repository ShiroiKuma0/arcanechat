<?xml version="1.0" encoding="utf-8"?>
<!-- shiroikuma fork (Step 7): "Filled card" chat-list style - rounded card with a subtle tint
     fill and an accent border. Ripple + state_selected preserved, clipped to the rounded shape. -->
<ripple
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorAccent">
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" />
            <corners android:radius="12dp" />
        </shape>
    </item>
    <item>
        <selector>
            <item android:state_selected="true">
                <shape android:shape="rectangle">
                    <solid android:color="@color/def_primary_alpha33" />
                    <stroke android:width="1.5dp" android:color="?attr/colorAccent" />
                    <corners android:radius="12dp" />
                </shape>
            </item>
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="@color/card_fill_tint" />
                    <stroke android:width="1.5dp" android:color="?attr/colorAccent" />
                    <corners android:radius="12dp" />
                </shape>
            </item>
        </selector>
    </item>
</ripple>
