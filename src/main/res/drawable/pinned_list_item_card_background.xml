<?xml version="1.0" encoding="utf-8"?>
<!-- shiroikuma fork (Step 5): pinned chat rows as rounded cards too - same accent border, but the
     pinned tint fill is kept so pinned chats stay visually distinct from normal ones. -->
<ripple
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorAccent">
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" />
            <corners android:radius="12dp" />
        </shape>
    </item>
    <item>
        <selector>
            <item android:state_selected="true">
                <shape android:shape="rectangle">
                    <solid android:color="@color/def_primary_alpha33" />
                    <stroke android:width="1.5dp" android:color="?attr/colorAccent" />
                    <corners android:radius="12dp" />
                </shape>
            </item>
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="@color/pinned_bg" />
                    <stroke android:width="1.5dp" android:color="?attr/colorAccent" />
                    <corners android:radius="12dp" />
                </shape>
            </item>
        </selector>
    </item>
</ripple>
