<?xml version="1.0" encoding="utf-8"?>
<!-- shiroikuma fork (Step 10): subgroup header inside a section. Non-selectable; the accent-coloured
     title carries a text-width underline. Indented one step (between the section header and items). -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingLeft="24dp"
    android:paddingEnd="?android:attr/scrollbarSize"
    android:paddingRight="?android:attr/scrollbarSize"
    android:paddingTop="14dp"
    android:paddingBottom="4dp">

    <TextView
        android:id="@android:id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/shiroikuma_subheader_underline"
        android:paddingBottom="3dp"
        android:textColor="?attr/colorAccent"
        android:textSize="13sp"
        android:textStyle="bold"
        android:textAllCaps="true"
        android:letterSpacing="0.04" />

</LinearLayout>
